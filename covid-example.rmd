---
title: Covid plots
author: Leon
date: '2021'
output: html_document
---

```{r}
library(ggplot2)
library(tidyverse)
library(scales)

cc_list <- readRDS("comp-comp_covid.rds")
```

# Data

```{r}
cc_list$Data
```

# New cases per day for each region

```{r}
cc_list$`New cases per day for each region`
```

# New cases per day for each region facet

```{r}
cc_list$`New cases per day for each region facet`
```

# Cases against Death

```{r}
cc_list$`Cases against Death`
```

# Cases against Death facet

```{r}
cc_list$`Cases against Death facet`
```
